<h1>OpenGL ES Guide <sup><span class="badge badge-info">beta</span></sup></h1>
<br>
<ol>
  <li>Create your Delight account and application.</li>
  <br>
  <li>Copy the app token.</li>
  <br>
  <li>Get the Delight framework from GitHub<br>
    <code>git clone git@github.com:delightio/ios.git</code>
  </li>
  <br>
  <li>Add <code>Delight.framework</code> to your target along with the following:
    <ul>
      <li type='a'>AssetsLibrary</li>
      <li type='a'>AVFoundation</li>
      <li type='a'>CoreGraphics</li>
      <li type='a'>CoreMedia</li>
      <li type='a'>CoreVideo</li>
      <li type='a'>OpenGLES</li>
      <li type='a'>QuartzCore</li>
      <li type='a'>SystemConfiguration</li>
    </ul>
  </li><br>
  <li>In your build settings, add <code>-ObjC</code> to "Other Linker Flags".</li><br>
  <li>In your application delegate,<br><br>
    <code>#import &lt;Delight/Delight.h&gt;</code> and<br><br>
    put the following in the <code>application:didFinishLaunchingWithOptions:</code>:
    <br><br>
    <code>
      [Delight startOpenGLWithAppToken:@"<strong>Your App Token</strong>"];
    </code>
  </li><br>
  <li>In your render loop, add the following line after drawing but before calling <code>-[EAGLContext presentRenderbuffer:]</code>:<br><br>
  	<code>
      [Delight takeOpenGLScreenshot:glView];
    </code><br><br>
    where glView is your OpenGL view.<br><br>
    If you're using multiple renderbuffers, specify which one to use by calling the following method instead:<br><br>
    <code>
	[Delight takeOpenGLScreenshot:glView colorRenderbuffer:renderbuffer];
    </code>

</ol>
