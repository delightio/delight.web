<div class="row">
  <div class="span10">
    <span style="font-size:x-large">In celebration of our official launch, we are <strong>doubling the credits</strong> for all packages. Sign up now to take advantage of this limited time offer.</span>
  </div>
</div>
<br/>
<h2>Please select a package from below:</h2><br/>
<div class="row-fluid center">
  <% PAYMENT_CONFIG['plans'].reverse_each do |payment| %>
    <a href="/apps?credit=<%= payment['name'] %>" class="span3 btn" style="margin-bottom: 15px">
    <h3><%= payment['display'] %></h3>
    <br/>
    <p>
    <span style="font-size:xx-large">$<%= payment['price'] %></span>
    <br/><br/>
    per <strike><%= payment['credit']/2 %></strike> <strong><%= "#{pluralize(payment['credit'], 'credit')}" %></strong>
    </p>
    <% if payment['name'] == 'volume' %>
      <p>
        <span style="font-size:large">Best deal!</span>
      </p>
    <% end %>
  </a>
  <% end %>
</div>
<div class="row-fluid">
  <div class="span10">
    <span style="font-size:large">
      <strong>Questions?</strong> <a href="mailto:info@delight.io"> Email</a>, <a href="https://twitter.com/intent/tweet?screen_name=delightio">tweet</a> or call us anytime at <strong>+1 (415) 413-7425</strong>.
    </span>
    <br><br>
    <div class="alert alert-info">
      <ul>
        <li>1 credit per recorded session</li>
        <li><%= Account::FreeCredits %> free credits for each new sign up</li>
        <li>All transaction are in USD and we accept all common credit cards</li>
        <li>Purchased credits never expire</li>
        <li>100% Satisfaction Money Back Guarantee.</li>
      </ul>
    </div>
  </div>
</div>