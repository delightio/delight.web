

                  <div class="round-btn-wrapper" id="session-recorder-btn">

                    <%= link_to(app_update_recording_path(:app_id => @app.id, :state => 'resume', :format => :json), :remote => true, :method => :put, :id => "session-recorder-start" ) do %>
                      <div class="round-btn-outer">
                        <div class="round-btn"><i class="control-btn icon-play"></i></div>
                      </div>
                    <% end %>

                    <%= link_to(app_update_recording_path(:app_id => @app.id, :state => 'pause', :format => :json), :remote => true, :method => :put, :id => "session-recorder-stop") do %>
                      <div class="round-btn-outer">
                        <div class="round-btn"><i class="control-btn icon-stop"></i></div>
                      </div>
                    <% end %>


                  </div>
