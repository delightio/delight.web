
  <div id="error-messages-container">  
    <% if flash[:notice] %>
    <div class="alert <%= (flash[:type].blank? ? "alert-success" : "alert-" + flash[:type]) %> fade in" data-alert="alert">
  	  <a class="close" data-dismiss="alert">×</a>
      <% if flash[:title] %>
        <strong><%= flash[:title] %></strong>
      <% end %>
      <%= flash[:notice] %>
    </div>
    <% end %>
  </div>

  <!-- <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p> -->
